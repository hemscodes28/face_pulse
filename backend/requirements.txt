# ============================================================
# Face Pulse Backend — Python Dependencies
# ============================================================

# Web framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Database — ORM and migration
sqlalchemy>=2.0.36,<3.0.0
alembic>=1.14.0,<2.0.0

# PostgreSQL drivers
# asyncpg  : async driver used by SQLAlchemy async engine (application)
# psycopg2-binary: sync driver used by Alembic (migration tool)
asyncpg>=0.30.0,<1.0.0
psycopg2-binary>=2.9.9,<3.0.0

# Configuration
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
aiosqlite>=0.20.0,<1.0.0          # SQLite async adapter for local tests without PostgreSQL
